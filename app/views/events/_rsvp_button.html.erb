<% if event.rsvp?(current_user) %>
  <%= button_to "Cancel RSVP", event_rsvp_path(event.rsvp_for(current_user)), method: :delete, remote: true %>
<% else %>
  <%= button_to "RSVP for this event", event_rsvps_path(event_rsvp: {event_id: event.id}), remote: true %>
<% end %>