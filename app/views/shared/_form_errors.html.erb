<% if object.errors.any? %>
  <div class="error_messages">
    <ul>
      <% object.errors.full_messages.each do |message| %>
        <div class='alert-box'>
          <%= message.sub( 'Concept descriptions d', 'D') %>
        </div>
      <% end %>
    </ul>
  </div> 
<% end %>