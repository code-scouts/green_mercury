<h3>Your Claimed Requests</h3>

<%= render 'requests_list', requests: current_user.claimed_requests, users: @claimed_users %>

<h3>Open Requests</h3>

<%= render 'requests_list', requests: current_user.open_requests, users: @open_users %>