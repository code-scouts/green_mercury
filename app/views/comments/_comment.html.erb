<%= comment.title %>
<%= comment.comment %>
<% if can? :participate, comment.get_project %>
  <%= link_to 'Reply', new_comment_path(commentable_id: comment.id, commentable_type: 'Comment') %>
<% end %>
<% if comment.comments.any? %>
  <%= render 'comments/comments', parent: comment %>
<% end %>